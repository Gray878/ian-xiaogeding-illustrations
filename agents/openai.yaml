interface:
  display_name: "Ian 小鸽钉配图视频"
  short_description: "生成小鸽钉正文配图和SRT同步手绘字幕视频"
  default_prompt: "Use $ian-xiaogeding-illustrations to 根据这份中文 SRT/TXT 字幕生成小鸽钉手绘错觉字幕视频。"
policy:
  allow_implicit_invocation: true
