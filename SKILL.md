---
name: ian-xiaogeding-illustrations
description: 生成 Ian 风格的中文正文配图，并把 SRT/TXT 字幕稿转成小鸽钉手绘错觉字幕动画视频。用于用户要求为中文文章、帖子、博客、Notion 文档、工作流文档、方法论、流程、结构、状态、隐喻或观点生成“怪诞”“小鸽钉”“手绘”“正文配图”“文章插图”“配图建议”“shot list”“去标题/改图”，或提供 .srt/.txt 字幕、要求根据字幕时间生成视频字幕、旁白同步动画、边讲解边手绘、B 档局部遮罩揭示等任务；默认使用小鸽钉 IP、纯白手绘、少量红橙蓝批注、简洁清爽但天马行空的视觉风格。
---

# Ian 小鸽钉怪诞正文配图

## 核心定位

为中文文章设计和生成 16:9 横版正文配图，也可把 SRT/TXT 字幕稿驱动成字幕同步的小鸽钉手绘错觉视频。目标不是做商业插画、PPT 信息图或可爱卡通，而是把文章里的关键判断、流程、结构、状态或隐喻，变成清爽、怪诞、有创意、可读但不说明书的手绘解释图和动画。

默认视觉 IP 是“小鸽钉”：灰白色圆团小鸽子，黑色或深灰小豆眼，橙黄色或深色短嘴，细腿，偶尔有短翅膀；可以背小工具包、小信封、小扳手、小插头，头顶或尾部可带一个很小的钉子、插头或小零件作为记忆点。小鸽钉必须像认真参与系统运转的荒诞小信使，负责传递信息、搬运概念、连接节点、修补断点，不能只是宠物、吉祥物或可爱装饰。

## 先读这些参考

按任务需要读取，不要一次塞满上下文：

- `references/style-dna.md`：风格 DNA、颜色、文字、禁忌。
- `references/xiaogeding-ip.md`：小鸽钉 IP 的形象、性格、动作库和禁忌。
- `references/composition-patterns.md`：结构类型、原创隐喻方法和反复刻规则。
- `references/prompt-template.md`：单张生图提示词模板。
- `references/qa-checklist.md`：生成后检查和迭代规则。
- `assets/examples/`：只作低频视觉校准，不进入默认生成路径；这些旧案例只参考线条密度、留白和颜色克制，不作为角色造型参考。不要照抄这些案例的构图、物件或标注。

## 工作流

### 0. 识别 SRT/TXT 字幕输入

如果用户提供 `.srt`，或提供 `.txt` 但内容包含 `-->`、`HH:MM:SS,mmm --> HH:MM:SS,mmm` 这类 SRT 时间码，先进入字幕视频流程，再做配图策略。

- 解析为 cue 列表：`index`、`start`、`end`、`text`。保留字幕原始时间，逗号毫秒可转换为小数秒；多行字幕合并为一句，明显多余空格可清理。
- 把 SRT 时间作为字幕和动画的唯一时间基准。不要按字数重新估时；视觉可以按段落合并，字幕必须按 cue 精准显示和消失。
- 生成字幕时间表：cue 范围、字幕文本、对应画面主题、小鸽钉动作、揭示节奏、是否需要橙色箭头或红蓝批注。
- 在视频里做可读字幕层：底部安全区优先，高对比，字号稳定，不遮挡小鸽钉主体和关键批注。每个 cue 只在自己的 `start` 到 `end` 可见。
- 如果没有旁白音频，仍按 SRT 驱动画面；如果后续生成或叠加音频，也以 SRT 时间线为准。

### 1. 消化正文

先读用户给的正文、链接、Notion 页面、Markdown 文件或截图内容。提炼：

- 核心观点是什么
- 哪些段落承担认知转折
- 哪些内容适合用图解释
- 哪些地方只适合文字，不需要图

不要平均配图。优先选择“认知锚点”，例如：核心判断、两个断点、输入输出闭环、分流、前后对比、一鱼多吃、承接路径、常见坑、角色状态变化。

### 2. 先出配图策略

如果用户只是说“分析怎么配图 / 思考哪些地方需要配图”，先给 shot list。每张图写清楚：

- 放在哪个段落后
- 图的主题
- 核心意思
- 结构类型
- 小鸽钉在图里做什么
- 建议元素
- 建议中文标注词

默认 4-8 张。文章很短时 1-3 张；长文也不要轻易超过 9 张。够用就好，避免把正文做成画册。

### 3. 单张生成

如果用户明确要求“生成 / 输出 / 做图 / 帮我生成”，不要停下来等确认；用内置 `image_gen` 每张单独生成。不要把多张图拼在一张里。

每张图只讲一个核心结构。提示词必须包含：

- 16:9 横版中文正文配图
- 纯白背景
- 黑色手绘线稿
- 少量红色/橙色/蓝色中文手写批注
- 大量留白
- 小鸽钉作为核心动作主体
- 禁止 PPT、商业插画、幼稚可爱、复杂架构、左上角类型标题

不要复刻过往案例。旧案例只提供风格密度和构图克制，不能作为小鸽钉造型参考，也不能直接复用“传送带断点 / 拉线 / 素材鱼 / 盖章工具箱 / 常见坑路径”等已有构图，除非用户明确要求复刻某张图。每次都要从当前文章重新发明一个奇怪但成立的隐喻。

### 4. 视频动画：默认 B 档手绘错觉

如果用户要“视频 / 字幕动画 / 边讲解边手绘 / 手绘效果 / 根据 SRT 做视频”，默认使用 B 档，不要等用户再次指定：

- 每张图拆成 10-25 个局部遮罩，按画面叙事路径逐段揭示。遮罩优先遮住具体物件、人物、箭头、标签、批注，而不是只做一整张大擦除。
- 揭示顺序：黑色主体结构线稿先出现，小鸽钉动作随后出现，关键物件再出现，橙色箭头最后串联，红色/蓝色批注和中文标签单独延迟出现。
- 用简化手/笔轨迹制造“正在画”的视觉错觉：笔尖要略领先当前遮罩，路径跟随物件轮廓或阅读方向，不要随机游走。
- 把每张图的 10-25 个遮罩分配到对应 SRT cue 或 cue 组内。短 cue 只换字幕，长 cue 承担主要绘制；跨 cue 的画面动作要连续。
- 转场保持白底纸面感，使用轻量擦除、纸张滑入或页面翻换；不要做商业片式炫光、3D 飞入或 PPT 切换。
- 抽帧检查：开头不能空白太久，局部内容不能提前露出，红蓝批注不能压住字幕，手/笔位置要和当前揭示区域相邻。

### 5. 检查与迭代

生成后检查 `references/qa-checklist.md`。如果出现以下问题，优先重生成或局部编辑：

- 小鸽钉只是装饰
- 画面太满
- 太像流程图/PPT
- 中文太多或错字严重
- 左上角出现“常见坑/流程图/系统架构图”等标题
- 画风太可爱、幼稚、死板
- 背景不是干净白底
- 视频字幕时间和 SRT 不一致，或字幕遮挡关键画面
- B 档遮罩太粗，只像整图擦除，不像局部手绘揭示

### 6. 保存交付

如果用户在 workspace 内工作，把最终图复制到：

```text
assets/<article-slug>-illustrations/
```

按顺序命名：

```text
01-topic-name.png
02-topic-name.png
```

保留原始生成文件，不要覆盖已有资产，除非用户明确要求替换。

视频任务同时保存：

```text
renders/<slug>.mp4
```

并保留字幕或脚本文件，便于复用：

```text
SCRIPT.md
captions.srt
```

## 输出口径

生成前的策略输出要短而准。生成后的交付要包含：

- 生成了几张
- 每张图的用途
- 保存路径
- 哪些图最稳，哪些图是可选
- 如果生成视频：视频时长、分辨率、字幕来源、B 档遮罩是否完成、校验结果

不要长篇解释风格理论；让图自己说话。
